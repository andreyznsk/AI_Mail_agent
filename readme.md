# AI-powered Resume ↔ Vacancy Matching & Cover Letter Generator

Сервис для автоматической обработки резюме и вакансий с генерацией персонализированных сопроводительных писем.

---

## 🚀 Цель проекта

Сервис автоматически:

- Загружает и парсит резюме пользователя.
- Структурирует данные резюме в доменную модель.
- Сопоставляет резюме с вакансиями из электронной почты.
- Объясняет степень соответствия (score + strengths/gaps).
- Генерирует персонализированные Cover Letters.

---

## 🎯 Целевая аудитория

Индивидуальные соискатели, желающие ускорить процесс отклика на вакансии и получать объяснения соответствия.

---

## 🧱 Функциональность MVP

### Входит в MVP

- Загрузка резюме при старте приложения.
- Парсинг резюме через LLM → доменная модель.
- Парсинг вакансий из email.
- Resume ↔ Vacancy matching (score + explanation).
- Генерация Cover Letter.
- Экспорт Cover Letter в `.txt`.
- Сохранение данных:
    - id вакансии
    - название компании
    - название должности
    - уровень ЗП
    - регион
    - описание вакансии
    - Cover Letter текст
- Дополнительно: таблица для хранения связки матчера к каждой вакансии (для дебага).
- Генерация черновика ответа на входящее письмо (в формате: вакансия + ссылка + текст письма).
- REST API (без UI).

### Не входит в MVP

- UI / frontend
- User accounts
- Multiple resumes
- Persistence / DB история
- Scoring history

---

## 🏗️ Архитектура

Стиль: **Modular Monolith**

**Основные домены:**

- Resume
- Vacancy
- MatchResult
- CoverLetter

**Ключевые абстракции:**

- `ResumeSource`
- `ResumeParsingService`
- `ResumeVacancyMatcher`
- `CoverLetterGenerator`
- `LlmClient`

**Хранилище:** отсутствует (MVP)  
**Docker:** поддержка контейнеризации  
**Ожидаемая нагрузка:** низкая

---

## 🧠 Используемые технологии

- **Java 21** (Eclipse Temurin)
- **Maven + Jenkins** (build & CI/CD)
- **Docker & Docker-compose**
- **LLM providers:**
    - Ollama (локально)
    - GigaChat
- **Антигаллюцинация:**
    - Структурированный JSON-вывод
    - Валидация результатов
    - Детерминированные конфиги (temperature=0)

---

## 🏁 Итерации разработки

1. **Resume ingestion & parsing ✅**
    - Загрузка резюме (файл / inline)
    - LLM-парсинг → Resume domain

2. **Vacancy ingestion**
    - Email parsing
    - Vacancy domain model

3. **Resume ↔ Vacancy matching**
    - `MatchResult` (score, strengths, gaps, summary)
    - LLM-based matcher
    - Deterministic validation rules

4. **Cover Letter generation**
    - Input: Resume + Vacancy + MatchResult
    - Output: CoverLetter text
    - Prompt contracts

5. **Database integration (планируется)**
    - PostgreSQL в Docker
    - Flyway миграции
    - Сущности DTO-ORM
    - Сохранение данных в БД

6. **Draft response generation**
    - Генерация черновика ответа на письмо
    - Пометка обработанных писем как прочитанных

---

## ⚠️ Ограничения

- Минимизация затрат на LLM
- Конфиденциальность: данные резюме не сохраняются
- GDPR-friendly: отсутствие долгосрочного хранения данных
- Каждое взаимодействие с LLM:
    - строгое соблюдение контрактов
    - валидация
    - fallback стратегия

---

## 🔧 Запуск проекта

1. Клонировать репозиторий:

```bash
#!/usr/bin/zsh
git clone https://github.com/andreyznsk/AI_Mail_agent.git
cd AI_Mail_agent
mvn clean install
docker build -t andreyznsk/app:mailagent.latest -f helm/DockerFile .
```

📝 Контакты и поддержка

Автор: Андрей Зайцев
GitHub: https://github.com/andreyznsk

📄 Лицензия

Проект лицензирован под MIT License.